meta {
  name: Cardápio Corrente
  type: http
  seq: 2
}

get {
  url: {{base_url}}/cardapio/corrente
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  base_url: http://localhost:8080
}
