meta {
  name: Pedido - Pay
  type: http
  seq: 3
}

post {
  url: {{base_url}}/pedidos/1/pagar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  base_url: http://localhost:8080
}

