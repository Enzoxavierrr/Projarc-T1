meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/clientes/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "joao@email.com",
    "senha": "senha123"
  }
}

vars:pre-request {
  base_url: http://localhost:8080
}

script:post-response {
  bru.setVar("token", res.body.token);
}

